import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.aulasonline',
  appName: 'AulasOnline',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
